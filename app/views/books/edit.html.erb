<%= form_with model: @book, url: update_book_path(@book.id), method: :patch do |f| %>
  <h1>Editing Book</h1>

  <div>Title</div>
  <%= f.text_field :title %>

  <div>Body</div>
  <%= f.text_area :body %>

  <%= f.submit 'Update book' %>



<% end %>